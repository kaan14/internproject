var firebase = require('./index');

var CRUD = {
    createCompany: function(companyName, businessType, companyPhone, companyWebsite, contactPerson, contactPersonLastname, emailAddress, companyDescription)
}